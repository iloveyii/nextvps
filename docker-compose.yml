services:
  adminer:
    image: adminer
    container_name: adminer-neon
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      ADMINER_DEFAULT_SERVER: "${POSTGRES_HOST}"
      ADMINER_DEFAULT_DB: "${POSTGRES_DATABASE}"
      ADMINER_DEFAULT_USERNAME: "${POSTGRES_USER}"
